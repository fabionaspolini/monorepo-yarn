{
  "name": "shared",
  "packageManager": "yarn@3.1.1",
  "private": true,
  "main": "dist/src/shared",
  "types": "dist/src/shared",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint src/*.ts",
    "test": "jest",
    "clean": "rm -r dist/*"
  }
}
